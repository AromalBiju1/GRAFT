# Core — keep in sync with pyproject.toml [project.dependencies]
chromadb>=1.0,<2.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.1
python-multipart>=0.0.9

# Dev / test (optional, install via: pip install -e \".[dev]\")
# pytest>=8.0
# httpx>=0.27
# ruff>=0.3
